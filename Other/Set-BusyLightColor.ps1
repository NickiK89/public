# Sideproject to change color on Kuando Busylight
# As all gamers know, more RGB = more FPS ;-)

#
[CmdletBinding()]
param(
)

function New-Stuff {
    param(
        [Parameter()]
        [string]$FilePath
    )

    # Enter code to do something here
}

# Get random number between 1 and 255
$Number = 250 #Get-Random -Minimum 1 -Maximum 255

# Increment random number

switch ($x) {
    condition {  }
    Default {}
} ($Number -le 255) {
    $Number++
    Write-Host $Number
    Start-Sleep -Milliseconds 250
}

for ($Number = $Number; $Number -le 255; $Number++) {
        
    } 
}
    