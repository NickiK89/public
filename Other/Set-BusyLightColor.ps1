# Sideproject to change color on Kuando Busylight
# As all gamers know, RGB = FPS ;-)

#
[CmdletBinding()]
param(
    # Mandatory parameters

    [Parameter(Mandatory)]
    [string]$FilePath
)

function New-Stuff {
    param(
        [Parameter()]
        [string]$FilePath
    )

    # Enter code to do something here
}